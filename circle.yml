machine:
  environment:
    USER_NAME: ryanfreeth
    USER_EMAIL: ryan.freeth@gmail.com
  ruby:
    version: 2.3.1
  node:
    version: 5.3.0
dependencies:
  pre:
    - gem install bundler -v 1.12.5
compile:
  override:
    - git config --global -l # debug
    - git config --global user.email circleci@circleci
    - git config --global user.name CircleCI
    - bundle exec rake deploy
test:
  override:
    - echo noop
general:
  branches:
    ignore:
      - master
