---
pageable: true
per_page: 4
---
<% content_for(:title) { } %>
<% content_for(:section) { 'I make things' } %>

<section id="hero">
  <header>
    <h1>Hello, I'm Ryan. I lead teams and build web apps.</h1>
  </header>
  <section>
    <p>Writing server-side apps with C# is hard. I know, I've been at it for nearly ten years. You want to get there faster? I can help.</p>
  </section>
</section>

<h2 class="margin-bottom">New? Start here</h2>
<section class="index-posts margin-top">
  <% page_articles.each do |article| %>
    <article class="index-post">
      <header>
        <h4>
          <%= link_to article.title, article %>
        </h4>
        <% if article.data.summary %>
          <p class="alt-title subtle"><%= article.data.summary %></p>
        <% end %>
      </header>
    </article>
  <% end %>
</section>
